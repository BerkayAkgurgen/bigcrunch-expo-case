export const cardStyles = {
  cardWrapper: (color) => ({
    paddingVertical: 24,
    paddingHorizontal: 20,
    backgroundColor: color,
    borderRadius: 20,
    position: "relative",
    overflow: "hidden",
  }),
  wave: { position: "absolute", bottom: 0, right: 0 },
};
